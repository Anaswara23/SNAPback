// Placeholder stub for Roshini's receipt parser
function parseReceiptText(rawText) {
  // Extract items and price from raw OCR text
  // Returns: [{name: string, price: number}]
  return [];
}

module.exports = { parseReceiptText };
